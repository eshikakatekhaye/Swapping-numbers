#include<stdio.h>
int main()
{
    int x,y;
    printf("Enter two numbers:");
    scanf("%d%d",&x,&y);
    printf("Numbers before swapping are: x=%d and y=%d",x,y);
    x=x+y;
    y=x-y;
    x=x-y;
    printf("Numbers after swapping are: x=%d and y=%d",x,y);
    return 0;
}
